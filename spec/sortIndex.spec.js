var expect = require('chai').expect;
var sortIndex = require('../advancedLowbar').sortIndex;

describe('sortIndex', function() {
  it('is a function', function() {
    expect(sortIndex).to.be.a('function');
  });

});